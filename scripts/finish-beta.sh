#!/usr/bin/env bash
# Kept only so old local instructions fail safely.
set -euo pipefail

echo "finish-beta.sh is retired."
echo "Use ./scripts/release.sh prepare, deploy-preview, and deploy-production."
exit 1
